# FastAPI + Uvicorn
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Celery + Redis
celery[redis]>=5.4.0
redis>=5.0.0

# Docker SDK (for container orchestration)
docker>=7.1.0

# GPU monitoring
pynvml>=12.0.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# HTTP client
httpx>=0.27.0

# File uploads (Form + UploadFile in FastAPI)
python-multipart>=0.0.9
